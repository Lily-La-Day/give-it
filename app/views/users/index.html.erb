
<h1>All Users</h1>
<%= link_to 'Sign-Up Here', new_user_path %>

<table>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>

      <td><%= link_to 'Show', user_path(user) %></td>
    </tr>
  <% end %>
</table>
